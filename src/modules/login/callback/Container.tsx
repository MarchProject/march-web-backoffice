import { useControllerCallback } from './controller'

const ContainerCallback = () => {
  useControllerCallback()

  return <div className="h-screen w-screen"></div>
}

export default ContainerCallback
