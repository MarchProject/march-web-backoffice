// import { tokenBuyRoute, tokenViewRoute } from '../constant'
// import { getPathParams } from './url'

describe('Url', () => {
  describe('getPathParams', () => {
    //
    // test('get abc from /investor/token/abc/', (done) => {
    //   const { symbol } = getPathParams(
    //     tokenViewRoute.regex,
    //     '/investor/token/abc/',
    //   )
    //   expect(symbol).toEqual('abc')
    //   done()
    // })
    // test('get abc from /investor/token/abc/buy', (done) => {
    //   const { symbol } = getPathParams(
    //     tokenBuyRoute.regex,
    //     '/investor/token/abc/buy',
    //   )
    //   expect(symbol).toEqual('abc')
    //   done()
    // })
  })
})
